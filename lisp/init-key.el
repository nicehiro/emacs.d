;;; -*- lexcial binding: t -*-

;;; show key map in current buffer
(use-package which-key
  :ensure t
  :custom
  (which-key-mode)
  (which-key-setup-minibuffer))

(provide 'init-key)
;;; init-key.el ends here
